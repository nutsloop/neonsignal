[wrap-git]
url = https://github.com/erthink/libmdbx.git
revision = stable
depth = 1
# Use native meson.build from packagefiles to avoid CMake 4.x bug on macOS
patch_directory = libmdbx

[provide]
libmdbx = mdbx_dep
